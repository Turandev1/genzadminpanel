# Backend URL must include /api/v1 and use HTTPS outside local development.
VITE_ADMIN_API_URL=http://localhost:8080/api/v1
VITE_APP_ENV=development

# Demo data is honored only by Vite DEV builds. Production always uses the API.
VITE_ADMIN_DEMO_MODE=true
